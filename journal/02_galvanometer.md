# Запись №2: Оживление Искры (Эпоха 1)
**Дата:** 19 декабря 2025

## 🚀 Что сделано

### 1. Инфраструктура и Архитектура
* **Веб-сервер:** Осуществлен полный переход на **Flask**. Теперь это полноценное веб-приложение.
* **Шаблонизация:** Код разделен на логику (`app.py`) и представление (`templates/`). HTML больше не "зашит" внутри Python-строк.
* **API:** Реализован эндпоинт `/api/toggle_switch` для обмена JSON-данными между фронтендом и бэкендом без перезагрузки страницы.

### 2. Визуализация (UI/UX)
* **Гальванометр:** Создан кастомный прибор на чистом CSS/HTML.
* **Анимация:** Реализована "живая" стрелка с инерцией (CSS `transition`), реагирующая на импульсы тока.
* **Управление:** Добавлен **слайдер напряжения** (1–100В), позволяющий пользователю менять мощность "батареи" в реальном времени.

### 3. Физическая модель (Логика)
* **Реализована передача параметров:** напряжение первичной цепи масштабируется в угол отклонения стрелки.
* **Программно заложено поведение индукции:** Система реагирует только на *изменение* состояния (событие переключения), имитируя производную $d\Phi/dt$. Постоянный ток игнорируется кодом, что эмулирует физическую реальность.

## 🚧 Текущие ограничения (Technical Debt)
* **Рассинхронизация UI и Бэкенда (State Sync):**
    * Данные о прогрессе сохраняются в памяти сервера (глобальная переменная), но **не передаются** на фронтенд при загрузке страницы.
    * *Симптом:* При возврате в эксперимент счетчик визуально сбрасывается на 0 (хотя сервер помнит прогресс) и обновляется только после первого действия.
    * *Симптом:* Слайдер напряжения всегда сбрасывается на 10В при обновлении страницы.
* **Дискретная физика:** Симуляция работает по событиям (клик -> расчет), а не в реальном времени. Мы видим результат (ток), но не видим процесс нарастания.
* **Невидимое поле:** Мы наблюдаем следствие (движение стрелки), но причина (магнитное поле) остается скрытой.

## 🔭 Следующие шаги
* **Визуализация поля:** Использовать HTML5 Canvas для отрисовки магнитных силовых линий.
* **Анимация поля:** Воспроизвести динамику магнитного потока при изменении тока.